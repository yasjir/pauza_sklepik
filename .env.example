# Skopiuj ten plik jako .env i uzupełnij wartości

# WYMAGANE: losowy klucz do podpisywania sesji
# Wygeneruj np. przez: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=zmien-mnie-na-losowy-klucz-32-znaki

# OPCJONALNE: zewnętrzna baza danych (domyślnie SQLite w ./data/sklepik.db)
# Odkomentuj i uzupełnij tylko jeśli używasz PostgreSQL/MySQL:
# DATABASE_URL=postgresql://user:password@host:5432/sklepik
